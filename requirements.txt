numpy
h5py>=3.7.0              # 2.10.0 causes issues on modern Python
matplotlib>=3.5
typing-extensions>=4.0
scipy>=1.7
pandas>=1.3
scikit-learn>=0.24
tqdm
pytest                   # you can pin later if needed
black                    # uses latest safely
pylint                   # uses latest safely